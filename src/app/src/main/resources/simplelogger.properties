# Configuration for slf4j-simple, the binding used to satisfy sqlite-jdbc's
# slf4j-api dependency. slf4j-simple defaults to INFO on stderr, which is
# noisier than this project wants for a dev CLI tool; WARN keeps it quiet
# unless something is actually wrong.
org.slf4j.simpleLogger.defaultLogLevel=warn
